--> git clone url = creates local repository.
--> git checkout -b branch_name = creates branch.
--> git checkout branch_name = switch branches.
--> git add = stage the changes of directory.
--> git commit -m = commit changes with message.
--> git push = upload to remote repository.
--> git pull = get the code from remote repository.
--> pwd = present working directory.
